#include <cassert>
#include <vector>

void check_properties(const std::vector<std::vector<int>>& triangle) {
    assert(0);
}

int main(int argc, char const *argv[])
{
    check_properties({});
    return 0;
}
